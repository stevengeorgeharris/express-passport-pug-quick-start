A Node application which uses:
* Express
* Passport
* Pug
* MongoDB
* Mongoose

Update `config/db.js` to point at your database.

Run `npm install && npm start` to get started.

Visit `localhost:1892/signup` to create a user.
