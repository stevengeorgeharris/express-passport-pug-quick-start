extends ./layout.pug

block content
    form(name="add-estimation", action="/signup", method="post")
        div.input
            label Email
            input(type="text", name="email")
        div.input
            label Password
            input(type="password", name="password")
        div.actions
            input(type="submit", value="Submit")
